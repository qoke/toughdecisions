Use the absolute rubric: the same five criteria (grounding and calibration; context and values fidelity; decision insight; practical robustness; role execution), scored 0–4 with the same anchors (0 = material failure; 2 = useful, with an important omission; 4 = handles the difficult features well; 1 and 3 = intermediate). Same flag types, same supporting-passage and weakness rules, and acceptance notes as constraints, not a prescribed answer. See graders/absolute.md for the full rubric.

Return JSON matching the schema.
